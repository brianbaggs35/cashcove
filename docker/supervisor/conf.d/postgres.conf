[program:postgres]
command=postgres -D /data/postgres
user=postgres
priority=10
autorestart=true
; SIGINT is Postgres' "fast" shutdown: roll back open transactions and exit cleanly.
stopsignal=INT
stopwaitsecs=60
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
redirect_stderr=true
