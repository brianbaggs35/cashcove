# hadolint configuration (https://github.com/hadolint/hadolint)
ignored:
  # Packages come from the vendors' own repositories (PGDG, nginx.org, Debian security)
  # so every build picks up the latest patched release; pinning exact apt versions would
  # hold back security fixes and break when a repository drops an old version.
  - DL3008
